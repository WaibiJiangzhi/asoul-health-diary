# JavaScript 重构计划

## 盘点基线（迁移前）

迁移开始时，`app.js` 约 4730 行，包含 135 个顶层函数。数据版本迁移已经独立在 `data-model.js`，但应用启动、事件绑定、状态持久化、页面渲染、弹窗、图片导出和 PWA 仍共享同一个闭包。

| 功能领域 | 当前函数数 | 主要范围 | 风险判断 |
| --- | ---: | --- | --- |
| 启动、事件、状态读取与规范化 | 31 | `app.js:1–990` | 高：决定旧数据能否读取和保存 |
| 个人资料 | 2 | `app.js:991–1012` | 低，但依赖共享状态和头像资源 |
| 倒计时与进度目标 | 21 | `app.js:1013–1468` | 中：渲染与保存耦合 |
| 空间 | 16 | `app.js:1469–1765` | 高：日程和曲线共同依赖活动空间 |
| 日程、周报与图片导出 | 60 | `app.js:1766–3225` | 最高：函数最多、编辑与导出交叉 |
| 曲线、节点与图片导出 | 32 | `app.js:3226–4198` | 高：SVG、画布和节点状态交叉 |
| 冷笑话、头像与备份 | 8 | `app.js:4140–4293` | 备份高风险，其余较低 |
| 通用格式化和校验 | 34 | `app.js:4294–4542` | 低：大部分是无副作用纯函数 |
| 导航、PWA、资源预载与提示 | 5 | `app.js:4543–4730` | 中：依赖浏览器生命周期 |

目前最长的函数是 `renderChartSvg`（约 190 行）、`createChartCanvas`（约 148 行）、`bindEvents`（约 111 行）、`createWeeklySummaryCanvas`（约 110 行）和 `normalizeState`（约 107 行）。这些函数不应作为第一批迁移对象。

## 当前进度

- 第四阶段 JavaScript 整理已完成。`app.js` 从盘点时约 4730 行降到 4484 行，并保留为唯一应用入口和 DOM 控制层。
- `app-utils.js`、`backup-codec.js`、`milestone-domain.js`、`chart-domain.js` 和 `schedule-domain.js` 通过冻结的全局对象暴露经典脚本边界，继续兼容本地启动、普通网页和 PWA。
- 所有业务状态修改统一经过 `persistState`，由它决定立即/延迟保存和局部刷新；空间切换、创建、编辑和删除统一经过 `persistSpaceChange` 协调目标、日程与曲线。
- 原 111 行的 `bindEvents` 现在只负责调用 6 个领域事件入口；原约 190 行的 `renderChartSvg` 已降到 56 行，曲线坐标与缩放是可单测纯逻辑；周汇总画布也已拆成背景、标题、目标和七日绘制步骤。
- `normalizeState` 明确保留在入口内：它仍依赖空间模板、表情资源、ID 生成与日程补全，强行移出只会把隐式依赖换成参数堆叠。旧数据迁移继续由 `data-model.js` 独立负责。
- 自动测试覆盖通用工具、当前/旧备份、目标与进度、曲线几何、AI 日程解析与顺延、数据迁移及项目完整性。桌面和手机回归覆盖五页导航、空间跨页协调、曲线缩放、领域弹窗、周报和现有完整示例数据。
- 当前 PWA 缓存版本为 `asoul-life-v66-0868`。

## 不变约束

1. 保持 `asoul-health-diary-v1`、冷笑话、提示音和备份时间的本地存储键不变。
2. 保持状态版本、字段名、旧版本迁移和 JSON 备份格式不变。
3. 保持脚本可通过本地启动器、普通网页和 PWA 缓存加载；暂不强制改成 ES Module。
4. 保持所有按钮、弹窗、键盘关闭、手机导航和导入导出行为不变。
5. 每批只迁移一个可验证边界；旧实现删除和新实现接入必须在同一批完成。

## 目标边界

- `app-utils.js`：无状态的日期、数字、文本转义和曲线值格式化。
- `backup-codec.js`：只负责备份包生成、JSON 解析与最外层结构识别，不操作本地存储或页面。
- `milestone-domain.js`：倒计时/进度目标的百分比、关联空间、排序与增减计算。
- `chart-domain.js`：曲线范围、点坐标、平滑路径、标签密度与缩放计算。
- `schedule-domain.js`：记录统计、任务状态、AI 模板解析与日程顺延。
- `data-model.js`：只负责版本迁移，继续保持对 DOM 和本地存储零依赖。
- `app.js` 状态与持久化边界：保留 `load / normalize / save`，由 `persistState` 统一业务写入和视图刷新。
- `app.js` 页面控制层：保留 DOM 渲染、弹窗、画布绘制、浏览器 API、导航和 PWA 外壳。

不要求每个领域都单独成文件。如果抽取后仍需要大量隐式全局变量，应先在 `app.js` 内建立控制器或依赖对象，而不是为了文件数量继续拆分。

## 迁移顺序

1. **通用纯函数（已完成）**：建立 `app-utils.js` 和独立单元测试；这是首个低风险切片。
2. **状态与备份护栏（已完成）**：为当前版本、旧示例备份、无效数据和未来版本拒绝补齐测试，并建立最小备份编解码边界。
3. **个人与目标领域（已完成）**：目标纯计算独立，个人输入及目标操作统一使用持久化和局部刷新入口。
4. **空间协调层（已完成）**：活动空间变化集中协调目标、日程、曲线和空间选择器。
5. **曲线领域（已完成）**：数据几何、SVG 结构、画布步骤和 DOM 事件边界明确，缩放与范围有独立测试。
6. **日程领域（已完成）**：AI 文本、顺延和统计独立；日编辑、周报和两套图片导出各自保留清晰职责。
7. **事件入口与应用外壳（已完成）**：总事件入口按领域委派，启动、导航、PWA 与跨页协调仍只有一处。

每完成一批，都运行完整性测试、数据迁移测试和该领域的定向浏览器回归；不因为重构顺便改变视觉或交互。
