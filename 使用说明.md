# Asoul 一个魂生活日记

直接双击 `Asoul一个魂健康日记.html` 即可使用；也可以双击 `Asoul一个魂健康日记.cmd`。入口文件为了兼容旧分享包保留原名，页面本身已经升级为“生活日记”。

页面不需要安装软件，也不需要联网。个人资料、健康 / 考研 / 工作三个空间的周计划、每日记录、折线图和笔记都会自动保存在当前浏览器中。

## 三个生活空间

- **健康**：训练日 / 休息日，包含饮食安排、饮食记录和体重字段，适合健身、跑步、作息与身体状态记录。
- **考研**：学习日 / 休息日，包含学习重点、复盘总结和科目任务，适合学习时长、刷题量、正确率与模考成绩记录。
- **工作**：工作日 / 休息日，包含工作重点、完成总结和事项交付，适合日程、深度工作时长和完成任务数记录。

每个空间的周计划、周报和折线图彼此隔离。切换空间不会删除或覆盖其他空间的数据。旧版健康日记第一次打开时会自动升级，原有周计划和图表全部归入“健康”空间。

## 每周计划与 AI 复盘

1. 先在“今天，想推进哪一条生活线？”中选择空间。
2. 点击“新建周计划”，选择开始日期，网页会自动建立 Day1 到 Day7。
3. 点击 Day 卡片切换日期；点击卡片里的“设置”，可切换活动日 / 休息日、整体状态和表情。
4. 页面上方的两项自由文字会随空间改变：健康是饮食，考研是学习重点与复盘，工作是工作重点与完成总结。
5. 每项任务的计划和实际完成分栏记录，使用 `√ / ×` 标记是否完成；提示音可随时关闭。
6. 一周结束后点击“显示周报”，可查看七日概览或复制 Markdown 纯文本。
7. 点击“导入周计划”里的“填入 JSON 模板”，把模板复制给 AI，并说明当前空间的目标、已有进度、每天可投入时间和限制。AI 返回的 JSON 会导入当前空间，不会串到其他空间。

同一个开始日期可以分别存在于健康、考研和工作空间；同一空间内不会创建两个相同开始日期的周计划。

## 折线图

每个空间都提供对应的快速示例：

- 健康：体重、跑步、俯卧撑、心情。
- 考研：有效学习时长、刷题量与正确率、模考成绩。
- 工作：深度工作时长、完成任务数。

点击“添加折线图”可自定义图表名称、横轴和最多三项指标。新增节点后，可以为每条折线分别选择贝拉、嘉然或乃琳表情；图表支持排序、缩放和横向查看。

## 数据与备份

- 自动保存的数据位于 Chrome / Edge 的本地存储中，不会自动生成到项目目录。
- 点击“备份”会下载 `Asoul生活日记-备份-日期.json`，内容包含三个空间的全部记录、个人资料、头像和网页中编辑的冷笑话。
- 点击“恢复”可导入新版生活日记备份，也兼容旧的 `asoul-health-diary` 健康日记备份。
- 清理浏览器、换电脑或大改网页前，建议先下载备份，并放入 `本地备份文件`。
- “清空记录”会清空三个空间的个人数据，但不会删除冷笑话源文件。

## 分享与发布

在项目目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\package-release.ps1 -Version v2.0.0
```

脚本会生成 `发布/Asoul一个魂生活日记-v2.0.0` 和同名 ZIP，只包含网页程序、三个轻量表情包及封面图片，不包含 `本地备份文件`、`每周截图记录`、`发布` 或大体积源素材。分享包不含当前浏览器里的任何个人记录。

## 文件说明

- `Asoul一个魂健康日记.html`：网页入口，保留旧文件名以兼容快捷方式。
- `app.js`：三个空间、周计划、周报、图表和备份功能。
- `data-model.js`：数据版本及旧备份迁移规则。
- `styles.css`、`weekly-planner.css`、`weekly-polish.css`：页面外观。
- `stickers.js`：轻量表情包清单。
- `冷笑话.js`：默认冷笑话内容。
- `图片/贝拉`、`图片/嘉然`、`图片/乃琳`：轻量表情包。
- `图片/其他图片`：封面人物图和 A-SOUL Logo。
- `tools/generate-stickers.ps1`：重新扫描三个表情包并生成 `stickers.js`。
- `tools/package-release.ps1`：生成不含私人记录的发布包。

请不要单独移动 HTML；上述网页文件和 `图片` 文件夹需要保持在一起。
