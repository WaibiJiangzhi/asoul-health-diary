# Asoul 一个魂健康日记

直接双击 `Asoul一个魂健康日记.html` 即可使用；也可以双击 `Asoul一个魂健康日记.cmd`。

这个页面不需要安装软件，也不需要联网。个人资料、每周计划、每日记录、折线图、节点和笔记都自动保存在当前浏览器的本地存储中，不会在项目文件夹里自动生成 JSON 文件。建议每周结束或大改网页前，点击右上角或页面底部“数据保险箱”里的“立即备份”，下载一份 JSON 文件；需要时可点“恢复”导入。

## 分享给别人

推荐在项目目录运行 `powershell -ExecutionPolicy Bypass -File .\tools\package-release.ps1 -Version v1.1.0` 自动生成分享包。脚本只会打包网页程序、三个轻量表情包和 `其他图片`，不会包含 `本地备份文件`、`每周截图记录`、`发布` 或 `图片/新加卷`。

手动打包时，需要包含 `Asoul一个魂健康日记.html`、`app.js`、`data-model.js`、三个 CSS、`stickers.js`、`冷笑话.js`、`图片/贝拉`、`图片/嘉然`、`图片/乃琳`、`图片/其他图片`、`使用说明.md` 和启动 CMD。对方解压整个 ZIP 后，双击 HTML 使用；不能只发送网页文件。网页继续修改后，旧 ZIP 也不会自动更新。

分享包只包含网页程序、冷笑话和表情图片，不包含你当前浏览器里的姓名、头像、每周计划、图表或节点，因此别人第一次打开时是空白日记。`本地备份文件` 属于你的个人资料，之后重新制作分享包时也不要放进去。

## 数据保存在哪里

- 自动保存的数据位于 Chrome / Edge 自己的浏览器本地存储中，不是项目目录里的文件。
- 只要继续使用同一个浏览器打开同一份网页，之前的内容就会自动出现。
- 点击“备份”后，浏览器才会生成名为 `Asoul健康日记-备份-日期.json` 的文件。请在浏览器弹出的保存位置中选择项目里的 `本地备份文件`；如果浏览器自动下载到“下载”文件夹，再手动移进去即可。
- 点击“恢复”，可以选择之前导出的 JSON 文件，把数据放回网页。
- 备份 JSON 包含个人档案、头像表情、每周计划与记录、每日表情、折线图与节点、节点表情和网页中编辑的冷笑话。
- 提示音开关、当前选中的日期、图表缩放和滚动位置属于临时界面偏好，不放进备份。
- 如果当前显示的是测试内容，可点击右上角“清空记录”。确认后会清空个人资料、每周计划、图表、节点和头像，但不会删除冷笑话。

## 每周计划与 AI 复盘

1. 在“每周日程安排”点击“新建一周”，选择起始日期。网页会自动建立 Day1 到 Day7。
2. Day1 到 Day7 会在页面内完整排列，不需要左右滑动。点击卡片主体只切换当天内容；点击卡片里的“设置”，才会打开当天设置，可切换“训练日/休息日”、选择完成状态和表情。
3. 每天只分为“训练日”和“休息日”。计划、实际完成、饮食和当天体重都能在页面上直接修改；体重会显示在截图周报与复制文本中。点击“添加一项安排”增加一行，点击行末的 `×` 删除。
4. 点击完成记录里的 `√` 或 `×` 会播放轻提示音；周计划标题旁的“提示音开/关”可以随时静音，选择会保存在当前浏览器。
5. 每项实际完成用 `√ / ×` 标记，后面的一栏填写配速、次数、身体感受等简短记录；再次点击已选中的符号可以取消。
6. 每天仍可选择“这期拉了 / 还不错 / 好好好”作为整体状态，并从贝拉、嘉然、乃琳表情包中选一个表情。跑前热身和跑后拉伸默认不作为单独记录项。
7. 周节点较多时可以左右滑动，也可以按开始日期所属的年份、月份筛选。选中一周后，可在周节点栏显示周报、设置开始日或删除。
8. 一周结束后点击“显示周报”，会打开适合截图的七日概览；点击“复制纯文本”可复制包含周期、每天状态、饮食、计划和实际完成情况的 Markdown 周报。
9. 点击“导入周计划”里的“填入 JSON 模板”，在下方文本框全选复制后交给 AI；同时告诉 AI 你的身体信息与目标（如体重、身高、年龄、减脂/增肌目标）、近期跑步/力量记录、每周可训练天数和不适限制。让 AI 严格按模板返回“下一周计划 JSON”；可选择保存好的 JSON 文件，也可把完整内容直接粘贴回来导入。完成状态和表情请在网页里自行选择。

这里把“安排”和“实际完成”分开，是为了避免周报只有一大段文字。跑量、配速、组数和平均次数会保留为独立条目，饮食与身体感受则保留自由文字，方便下周调整时同时参考数据和真实感受。

## 使用方法

1. 在顶部填写姓名、性别、身高和年龄；点击头像可从贝拉、嘉然、乃琳表情包中挑选头像，选好后点击“用这个表情”。
2. 在“每周日程安排”安排一周日程和每天的实际完成情况。
3. 点击“添加折线图”，填写图表名称、横轴和一至三项指标。每项指标会生成一条独立折线。
4. 在图表标题栏点击“＋ 新节点”，填写横轴值、各项指标和小笔记；多指标图可分别选中每一条线，再为它各自选择贝拉、嘉然或乃琳表情。跑步配速可输入 `5:30` 这样的格式。
5. 点击折线图上的任意节点，会在图表下方显示当天数值与笔记；点击“设置当前节点”即可修改或删除。
6. 图表标题栏的 `↑`、`↓` 可以调整折线图顺序。
7. 数据较多时，可在图表中选择“放大”“缩小”“看全局”；放大后可拖动横向滚动条或在触屏设备上左右滑动。
8. 点击冷笑话卡片里的“编辑笑话”，即可在网页中添加、修改和删除。网页修改会保存在本机，也可点击“导出冷笑话.js”下载新的源文件。
9. 也可以直接编辑同目录下的 `冷笑话.js`；网页中点击“恢复文件里的笑话”即可重新读取本次打开页面时的文件内容。

## 文件说明

- `Asoul一个魂健康日记.html`：网页入口。
- `styles.css`：页面外观。
- `app.js`：日记、图表、备份恢复等功能。
- `data-model.js`：数据版本号和旧备份迁移规则。
- `stickers.js`：本地表情包清单。
- `冷笑话.js`：默认冷笑话内容，可直接编辑。
- `图片/贝拉`、`图片/嘉然`、`图片/乃琳`：轻量表情包。
- `图片/其他图片`：封面人物图和 A-SOUL Logo。
- `tools/generate-stickers.ps1`：根据三个轻量表情包重新生成 `stickers.js`。
- `tools/package-release.ps1`：生成不含私人记录和大体积源素材的发布包。
- `本地备份文件`：完整网页备份 JSON 的建议保存位置；恢复数据时从这里选择文件，不要放进分享包。
- `每周截图记录`：手动保存的周报截图，可能包含个人记录，分享网页程序时不要放进公开压缩包。
- `Asoul一个魂健康日记.cmd`：Windows 快捷启动文件。

请不要单独移动 `Asoul一个魂健康日记.html`；上述网页文件和 `图片` 文件夹需要保持在一起。
